# test port config
device:
  name: Test_Inverter
  id: A123456789
  model: 8048MAX
  manufacturer: MPP-Solar
  port:
    type: test
    identifier: A123456789
    # response_number: 3
    protocol: PI18

commands:
- command: POP1
  # trigger:
  #   every: 5
  outputs:
  - type: screen
    format: 
      type: table
      format: basic
      draw_lines: True
      # extra_info: True
      include_missing: False
      # filter: ^pv
loop: once